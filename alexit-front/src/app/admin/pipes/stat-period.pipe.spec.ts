import { StatPeriodPipe } from './stat-period.pipe';

describe('StatPeriodPipe', () => {
  it('create an instance', () => {
    const pipe = new StatPeriodPipe();
    expect(pipe).toBeTruthy();
  });
});
