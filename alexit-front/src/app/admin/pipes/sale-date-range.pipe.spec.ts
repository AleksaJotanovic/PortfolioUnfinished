import { SaleDateRangePipe } from './sale-date-range.pipe';

describe('SaleDateRangePipe', () => {
  it('create an instance', () => {
    const pipe = new SaleDateRangePipe();
    expect(pipe).toBeTruthy();
  });
});
