import { SaleByCodePipe } from './sale-by-code.pipe';

describe('SaleByCodePipe', () => {
  it('create an instance', () => {
    const pipe = new SaleByCodePipe();
    expect(pipe).toBeTruthy();
  });
});
