import { SaleByCtgPipe } from './sale-by-ctg.pipe';

describe('SaleByCtgPipe', () => {
  it('create an instance', () => {
    const pipe = new SaleByCtgPipe();
    expect(pipe).toBeTruthy();
  });
});
