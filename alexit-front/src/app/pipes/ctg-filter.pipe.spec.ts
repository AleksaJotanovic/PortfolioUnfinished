import { CtgFilterPipe } from './ctg-filter.pipe';

describe('CtgFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CtgFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
